name: Keepalive 24/7

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Warm always
        env:
          RENDER_URL: ${{ secrets.RENDER_URL }}
          WAKER_TOKEN: ${{ secrets.WAKER_TOKEN }}
        run: |
          echo "Warming…"
          curl -fsSL "$RENDER_URL/admin/warm?token=$WAKER_TOKEN" || true
